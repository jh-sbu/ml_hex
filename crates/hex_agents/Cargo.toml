[package]
name    = "hex_agents"
version.workspace = true
edition.workspace = true

[dependencies]
hex_core = { workspace = true }
rand     = { workspace = true }
burn     = { workspace = true, features = ["ndarray", "std"] }
